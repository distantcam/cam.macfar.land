---
layout: default
---

<header class="pt-5 mx-auto text-center lg:pt-24 lg:-mb-24 lg:max-w-5xl xl:max-w-7xl">

  <time datetime="{{ date | htmlDate }}"><i class="text-sm material-icons text-primary">calendar_today</i> {{ date | readableDate }}</time>

  <h1 class="pb-5 text-5xl leading-tight">{{ title }}</h1>

  <i class="inline-block text-base align-middle material-icons">local_offer</i>

  <ul class="inline-block">
  {% for i in range(1, tags.length) %}
    <li class="inline-block mx-2 text-sm font-bold uppercase text-primary">{{ tags[i] }}</li>
  {% endfor %}
  </ul>

  {% include "partial/page-head.njk" %}
  
</header>

<main class="relative px-10 pt-10 mx-auto bg-gray-100 dark:bg-gray-900 lg:min-h-24 lg:max-w-4xl xl:max-w-6xl">
  {% if unsplash_id %}
  <p class="absolute top-0 right-0 px-5 py-3 text-sm">Photo by <a href="https://unsplash.com/@{{ post_unsplash.user.username }}?utm_source={{ metadata.unsplash.utm_source }}&utm_medium=referral">{{ post_unsplash.user.name }}</a></p>
  {% endif %}

  <div class="m-auto prose-sm prose sm:prose md:prose-lg xl:prose-xl dark:prose-dark xl:max-w-prose-xl">
  {{ content | safe }}
  </div>

</main>
